{
  "name": "select-and-translate",
  "version": "0.0.6",
  "description": "Browser extension that translates text from any part of a webpage by selecting the area with your cursor",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --config vite.config.content.ts",
    "build:dev": "tsc && vite build --config vite.config.dev.ts && vite build --config vite.config.content.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.14.1",
    "@types/webextension-polyfill": "^0.12.3",
    "fs-extra": "^11.3.0",
    "typescript": "~5.7.2",
    "vite": "^7.0.3",
    "vite-plugin-static-copy": "^2.3.1"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0",
    "zod": "^4.0.5"
  }
}
